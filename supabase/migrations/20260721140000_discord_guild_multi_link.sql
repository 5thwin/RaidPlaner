-- =========================================================
-- 마이그레이션: 디스코드 서버 하나에 공대 여러 개 연동 허용
-- =========================================================
-- guilds.discord_guild_id가 unique라 디스코드 서버 하나엔 공대 하나만 연결할 수
-- 있었다. 여러 공대가 같은 디스코드 서버를 같이 쓰는 경우를 지원하기 위해
-- unique 제약을 없앤다(2026-07-21 사용자 확인). 이제 /파티현황 명령어는 그
-- 서버에 연동된 공대가 여러 개면 "공대" 옵션으로 어느 공대인지 지정해야 한다
-- (discord-interactions Edge Function이 처리).
alter table public.guilds
  drop constraint guilds_discord_guild_id_key;
